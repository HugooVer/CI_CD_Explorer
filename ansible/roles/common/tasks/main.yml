---

- name: Testr cmd
  ansible.builtin.shell:
    echo "test"

- name: Test copy file
  ansible.builtin.copy:
    src: /srv/myfiles/foo.conf
    dest: /etc/foo.conf
    owner: foo
    group: foo
    mode: '0644'
